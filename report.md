# BugScan Analysis Report for https://github.com/tqrcisio/document-parser-api

**Generated on:** Tue, 16 Dec 2025 13:38:29 -03

---

## 🔴 High Severity
- `/home/tarcisio/.bugscan/repos/document-parser-api/main.py:33` – Path Traversal vulnerability in `output_file` parameter. The user-provided filename is directly concatenated with a base directory without sanitization, allowing an attacker to write files outside the intended 'data' directory by using path traversal sequences like '../'. This could lead to overwriting system files or application code.
- `/home/tarcisio/.bugscan/repos/document-parser-api/main.py:45` – Server-Side Request Forgery (SSRF) vulnerability. The `pdf_url` parameter is accepted and used to fetch a remote file without validation. An attacker can provide a URL pointing to internal services (e.g., 'http://127.0.0.1/admin', cloud metadata services), causing the server to make requests on their behalf and potentially exposing sensitive internal data or services.
- `/home/tarcisio/.bugscan/repos/document-parser-api/src/pdf_parser.py:37` – The 'source' parameter can be a URL, which is fetched by the server without validation. An attacker could provide a URL pointing to an internal service (e.g., 'http://127.0.0.1/admin', 'http://169.254.169.254/latest/meta-data'), leading to a Server-Side Request Forgery (SSRF) vulnerability. Input URLs should be validated against an allowlist of domains or configured to block requests to private/internal IP ranges.
- `/home/tarcisio/.bugscan/repos/document-parser-api/src/pdf_parser.py:42` – The 'output_path' parameter is used to write a file without proper sanitization. An attacker could use path traversal sequences (e.g., '../../etc/passwd') to write files outside the intended directory, potentially overwriting system files or placing malicious content in sensitive locations. The application must resolve the path and verify it is within a designated, secure base directory before writing.

## 🟡 Medium Severity
- `/home/tarcisio/.bugscan/repos/document-parser-api/main.py:27` – Potential Denial of Service (DoS) via large file uploads. The endpoint does not enforce a size limit on the uploaded `pdf_file`. An attacker could upload an extremely large file, consuming excessive disk space, memory, and CPU during processing, potentially leading to service unavailability.
- `/home/tarcisio/.bugscan/repos/document-parser-api/main.py:40` – Potential for blocking the event loop. The PDF parsing operation `pdf_parser.parse_to_markdown` is likely a CPU-bound or synchronous I/O task. Executing it directly within an `async` function blocks the server's event loop, preventing it from handling other concurrent requests and causing a significant performance bottleneck.
- `/home/tarcisio/.bugscan/repos/document-parser-api/main.py:45` – Potential Denial of Service (DoS) via remote URL. When processing `pdf_url`, there are no size limits or timeouts for downloading the remote file. An attacker can point to a very large file or a slow, non-responsive endpoint to exhaust server resources (network bandwidth, memory, CPU), leading to a DoS.
- `/home/tarcisio/.bugscan/repos/document-parser-api/src/pdf_parser.py:37` – The function processes PDF documents from a URL or local path without any size limits. A malicious user could provide a very large file, causing excessive consumption of memory, CPU, and network bandwidth during download and parsing. This can lead to a Denial of Service (DoS) for other users. Implement size limits and timeouts for remote resource fetching and processing.
- `/home/tarcisio/.bugscan/repos/document-parser-api/src/pdf_parser.py:45` – The code uses a broad 'except Exception as e:' clause, which catches all synchronous exceptions. It then re-raises them as a generic 'ValueError', masking the original error type (e.g., an 'OSError' during file write becomes a 'ValueError'). This makes debugging difficult and leads to misleading error information for the caller. It is better to catch more specific exceptions and handle them accordingly.

## 🔵 Low Severity
- `/home/tarcisio/.bugscan/repos/document-parser-api/main.py:32` – Inefficient object instantiation in request handler. A new `PDFParser` instance is created for every API request. If the class initialization is costly (e.g., loads large models or configurations), this pattern is inefficient. The parser should be instantiated once at application startup and reused across requests, possibly via FastAPI's dependency injection.
- `/home/tarcisio/.bugscan/repos/document-parser-api/main.py:34` – Ambiguous API behavior when both file and URL are provided. The endpoint accepts both `pdf_file` and `pdf_url`, but if both are sent, it silently prioritizes `pdf_file` and ignores `pdf_url`. The API should enforce mutual exclusivity to provide clear and predictable behavior for clients.
- `/home/tarcisio/.bugscan/repos/document-parser-api/main.py:42` – Potential `NameError` in `finally` block. The `tmp_path` variable is defined inside the `try` block. If an exception occurs during the creation of the `NamedTemporaryFile` (before `tmp_path` is assigned), the `finally` block will raise a `NameError` when trying to access `tmp_path`, masking the original, more informative error.
- `/home/tarcisio/.bugscan/repos/document-parser-api/main.py:43` – Overly broad exception handling silences errors. The use of `except Exception: pass` during temporary file cleanup is a bad practice. It suppresses all errors, hiding potential underlying issues like filesystem permissions that prevent cleanup, which could lead to resource leaks (e.g., disk filling up with temporary files).
- `/home/tarcisio/.bugscan/repos/document-parser-api/src/pdf_parser.py:3` – The 'requests' library is imported but not directly used in the file. While it might be a dependency of 'docling', this unused import can be confusing and adds unnecessary clutter. It should be removed if it is not directly used.
- `/home/tarcisio/.bugscan/repos/document-parser-api/src/pdf_parser.py:46` – When re-raising an exception, the original exception's context and traceback are lost. It is idiomatic Python 3 to use 'raise NewException from original_exception' to preserve the exception chain, which is invaluable for debugging. The line should be changed to 'raise ValueError(f"...") from e'.

