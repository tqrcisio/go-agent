# BugScan Analysis Report for /home/tarcisio/projects/singular/site-frontend

**Generated on:** Fri, 19 Dec 2025 15:38:41 -03

---

## 📊 Analysis Statistics
- **Total Prompt Tokens:** 58606
- **Total Response Tokens:** 32229
- **Estimated Cost:** $0.3955

---

## 🔴 High Severity
- `/home/tarcisio/projects/singular/site-frontend/app/components/Button/Button.vue:68` – When the component is rendered as a link (by providing the 'to' prop) and the 'disabled' prop is true, the link remains fully functional. The `disabled` attribute has no effect on `<a>` tags, so the link will still navigate and emit a 'click' event. The 'onClick' handler should prevent the event's default action if the component is disabled.
- `/home/tarcisio/projects/singular/site-frontend/app/components/Card/Card.vue:32` – The prop name 'new' is a reserved keyword in JavaScript. Using it as a prop name is strongly discouraged as it can lead to parsing errors or unexpected behavior, especially if props are destructured in the script. It should be renamed to something descriptive and non-reserved, such as 'isNewTab' or 'openInNewTab'.
- `/home/tarcisio/projects/singular/site-frontend/app/components/Carousel/Carousel.vue:14` – The carousel has no boundary detection. Users can scroll indefinitely past the beginning or end of the item list, resulting in a poor user experience with blank space. The navigation buttons should be disabled when the scroll position is at the start or end.
- `/home/tarcisio/projects/singular/site-frontend/app/components/Carousel/CarouselCard.vue:6` – The function `getImgUrl` is used in the template to resolve the background image URL, but it is not defined, imported, or otherwise made available to the component's script or template scope. This will result in a runtime reference error, causing the component to fail to render correctly.
- `/home/tarcisio/projects/singular/site-frontend/app/components/ConnectiCard/ConnectiCard.vue:4` – The `<img>` tag has an empty `alt` attribute. This is a critical accessibility issue as it provides no information to screen reader users about the image, which appears to be meaningful content (an event banner). A descriptive alt text should be provided, for example: `alt="Banner do evento CONNECTI 2025"`.
- `/home/tarcisio/projects/singular/site-frontend/app/components/Footer/FooterDesktop.vue:17` – The component uses standard `<a>` tags for internal navigation (e.g., '/saude-da-mulher'). In a Nuxt.js application, this causes a full page reload for every internal link clicked, bypassing the client-side router. This leads to a poor user experience, loss of application state, and significantly slower navigation. The `<NuxtLink>` component should be used for all internal links to enable fast, client-side routing and prefetching capabilities.
- `/home/tarcisio/projects/singular/site-frontend/app/components/Footer/FooterMobile.vue:4` – Standard `<a>` tags are used for internal navigation instead of Nuxt's `<NuxtLink>` component. This causes full-page reloads, bypassing the single-page application (SPA) behavior of Nuxt, which negatively impacts performance and user experience.
- `/home/tarcisio/projects/singular/site-frontend/app/components/Header/DesktopMenu.vue:7` – The component uses standard `<a>` tags for internal navigation (e.g., `<a href="/nossa-historia">`). In a Nuxt.js application, this forces a full-page reload for every navigation, bypassing the client-side routing benefits of the framework. This leads to a slower user experience and is against Nuxt best practices. Use the `<NuxtLink>` component for all internal links to enable fast, SPA-style navigation.
- `/home/tarcisio/projects/singular/site-frontend/app/components/Header/MobileMenu.vue:9` – Standard `<a>` tags are used for internal navigation instead of Nuxt's `<NuxtLink>` component. This causes a full page reload on every navigation, negating the benefits of a Single Page Application (SPA) and leading to a poor user experience and degraded performance.
- `/home/tarcisio/projects/singular/site-frontend/app/components/HealthPages/HealthPage.vue:8` – The use of `v-html` on `pageData.introduction.title` introduces a potential Cross-Site Scripting (XSS) vulnerability. If the `title` content comes from a CMS or user input and is not properly sanitized, an attacker could inject malicious scripts into the page.
- `/home/tarcisio/projects/singular/site-frontend/app/components/MainBanner/MainBanner.vue:18` – The `scrollTo` function directly accesses browser-specific APIs (`document`, `window`, `history`). In a Nuxt.js application, this code will execute on the server during Server-Side Rendering (SSR) and cause a crash, as these global objects are not defined in a Node.js environment. All DOM-related logic should be encapsulated within an `onMounted` hook or guarded with a `if (process.client)` check to ensure it only runs on the client-side.
- `/home/tarcisio/projects/singular/site-frontend/app/components/MainBanner/MainBanner.vue:24` – The line `history.replaceState(null, '', ' ');` incorrectly modifies the browser's URL, replacing the current path with a single space. This is likely unintended behavior that can break user navigation, bookmarks, and page reloads. If the goal was to remove a URL hash, a more appropriate method like `history.replaceState(null, '', window.location.pathname)` should be used.
- `/home/tarcisio/projects/singular/site-frontend/app/components/TextsCarousel/TextsCarousel.vue:54` – The carousel navigation controls are implemented using non-semantic `<p>` tags with `@click` handlers. This is a major accessibility issue as these elements are not focusable via keyboard, not recognized as interactive controls by screen readers, and do not follow standard button behaviors. They should be replaced with semantic `<button>` elements.
- `/home/tarcisio/projects/singular/site-frontend/app/components/contact/Contact.model.ts:1` – The interface is named `FormData`, which conflicts with the globally available `FormData` interface from the Web API. This can lead to naming collisions, confusion, and subtle bugs where developers might misuse one for the other, especially if this module is imported into a file that also uses the native FormData API. It is strongly recommended to rename it to something more specific, such as `ContactFormData`.
- `/home/tarcisio/projects/singular/site-frontend/app/components/contact/ContactCard.vue:29` – The `id="call-center"` is used on multiple elements (lines 7 and 29). HTML element IDs must be unique within a document. This is invalid HTML and causes accessibility issues, as the `aria-labelledby` attribute on the fourth section (line 25) will reference the first heading, leading to a mismatch between the label and the content.
- `/home/tarcisio/projects/singular/site-frontend/app/components/contact/Form.vue:246` – The form includes the `novalidate` attribute, which disables native browser validation for `required` fields and `type="email"`. However, there is no corresponding JavaScript-based validation logic in the `handleSubmit` function before the `submit` event is emitted. This allows the form to be submitted with empty required fields or invalid data, delegating all validation responsibility to the parent component and potentially leading to submission of incomplete or incorrect data.
- `/home/tarcisio/projects/singular/site-frontend/app/pages/agenda-de-eventos/index.vue:6` – The entire event schedule is hardcoded directly into the HTML template. This practice is inflexible, difficult to maintain, and not scalable. The event data should be managed as a data structure (e.g., an array of objects) within the <script> section and rendered dynamically using a `v-for` loop. This would also facilitate creating a reusable `Event` component.
- `/home/tarcisio/projects/singular/site-frontend/app/pages/agenda-de-eventos/index.vue:100` – A large constant `carouselItems` is declared and initialized with a significant amount of data, but it is never used anywhere in the component. This constitutes a large block of dead code that unnecessarily increases the JavaScript bundle size and adds cognitive overhead for developers maintaining the file.
- `/home/tarcisio/projects/singular/site-frontend/app/pages/blog/[slug].vue:36` – The `v-html` directive is used to render `post.content` directly. This is a potential Cross-Site Scripting (XSS) vulnerability if the content fetched from the API contains malicious scripts. All HTML content from external sources should be sanitized before being rendered.
- `/home/tarcisio/projects/singular/site-frontend/app/pages/blog/index.vue:153` – The type `any` is used for the `post` variable within the `map` function. This circumvents TypeScript's type safety, making the code prone to runtime errors and harder to maintain. The data from `GqlBlog` should be strongly typed based on the GraphQL schema.
- `/home/tarcisio/projects/singular/site-frontend/app/pages/blog/index.vue:173` – The type `any` is used for `post` and `category` variables when computing `postCategories`. This undermines the benefits of TypeScript, such as static analysis and autocompletion. Proper types should be defined for the GraphQL response.
- `/home/tarcisio/projects/singular/site-frontend/app/pages/contato/orcamento.vue:54` – The `fetch` call is not wrapped in a try...catch block. If the network request fails (e.g., server is down, network error) or if `res.json()` fails to parse (e.g., server returns a non-JSON 500 error page), the promise will reject, leading to an unhandled exception. This will prevent `loading.value` from being set to `false`, leaving the UI in a permanent loading state.
- `/home/tarcisio/projects/singular/site-frontend/app/pages/contato/orcamento.vue:54` – The API endpoint URL is hardcoded to 'http://localhost:8080'. This will fail in any environment other than local development (e.g., staging, production). Use Nuxt's runtime configuration (`useRuntimeConfig`) to manage environment-specific variables.
- `/home/tarcisio/projects/singular/site-frontend/app/pages/contato/trabalhe-conosco.vue:61` – The fetch call is not wrapped in a try/catch/finally block. If the network request fails or the server returns an error that prevents the promise from resolving, the function will throw an unhandled exception. This will leave the UI in a permanent loading state because 'loading.value = false' will never be executed.
- `/home/tarcisio/projects/singular/site-frontend/app/pages/dermatologia/index.vue:8` – The 'robots' meta tag is hardcoded to 'noindex,nofollow'. This will prevent search engines from indexing this page and following its links, which is likely not the intended behavior for a public-facing marketing page and severely impacts SEO.
- `/home/tarcisio/projects/singular/site-frontend/app/pages/formas-farmaceuticas/index.vue:230` – The `scrollTo` function incorrectly manipulates the browser's history by calling `history.replaceState(null, '', ' ')`. This replaces the current URL path with a single space, which can break page reloads, bookmarking, and relative path navigation. A correct implementation to remove a URL hash without affecting the path would be `history.replaceState(null, '', window.location.pathname)`.
- `/home/tarcisio/projects/singular/site-frontend/app/pages/index.vue:26` – The use of `v-html` on line 26 renders raw HTML from the `card.description` property. This is a significant security risk as it can lead to Cross-Site Scripting (XSS) attacks if the data source is ever changed from a static array to a dynamic API or CMS. It is recommended to use structured data and component slots instead of injecting raw HTML.
- `/home/tarcisio/projects/singular/site-frontend/app/pages/longevidade/index.vue:8` – The 'robots' meta tag is hardcoded to 'noindex,nofollow'. This instructs search engines to neither index this page nor follow any links from it, which is highly detrimental for SEO if this page is intended for public discovery. This might be a leftover from a development or staging environment.
- `/home/tarcisio/projects/singular/site-frontend/app/pages/martins-brasil/index.vue:51` – The `submitMartinsBrasil` function lacks proper error handling for its asynchronous operations. If the `fetch` request fails (e.g., network error, server error) or if `res.json()` fails to parse, the promise will reject, and the code will not proceed to `loading.value = false`. This will leave the form in a permanent loading state, preventing the user from retrying.
- `/home/tarcisio/projects/singular/site-frontend/app/pages/nossa-historia/index.vue:101` – The 'robots' meta tag is hardcoded to 'noindex,nofollow', which instructs search engines not to index this page or follow its links. This is highly detrimental for SEO and likely a mistake for a public-facing content page like 'Nossa História'.
- `/home/tarcisio/projects/singular/site-frontend/app/pages/nossas-unidades/index.vue:32` – The page's robots meta tag is hardcoded to 'noindex,nofollow', which prevents search engines from indexing and following links on this page. This could be a critical SEO issue if unintentional for a production environment.
- `/home/tarcisio/projects/singular/site-frontend/app/pages/nossas-unidades/index.vue:61` – The type 'SectionType' is used for the 'section' constant but is not defined or imported. This will cause a TypeScript compilation error.
- `/home/tarcisio/projects/singular/site-frontend/app/pages/nutricao/index.vue:10` – The page is hardcoded with a 'noindex,nofollow' robots meta tag. This will prevent search engines like Google from indexing this page and following its links, severely harming its SEO visibility. This is highly unusual for a primary content page and is likely a mistake unless the page is intentionally meant to be hidden from the public.
- `/home/tarcisio/projects/singular/site-frontend/app/pages/saude-da-mulher/index.vue:7` – The page is hardcoded with 'noindex,nofollow' for the robots meta tag. This will prevent search engines from indexing the page, which is likely not the intended behavior for a production page and can severely harm SEO.
- `/home/tarcisio/projects/singular/site-frontend/app/pages/saude-intestinal/index.vue:8` – The page is explicitly configured with 'noindex,nofollow' for its robots meta tag. This will prevent search engines from indexing the page and following its links, which is highly detrimental to SEO unless this page is intentionally meant to be kept private.
- `/home/tarcisio/projects/singular/site-frontend/app/pages/seja-um-franqueado/index.vue:108` – The robots meta tag is hardcoded to 'noindex,nofollow', which instructs search engines not to index this page or follow its links. If unintentional, this is critical for SEO and will prevent the page from appearing in search results.
- `/home/tarcisio/projects/singular/site-frontend/app/pages/seja-um-franqueado/index.vue:137` – The `submitFranchise` function lacks a try-catch block to handle potential errors from the `fetch` call (e.g., network errors) or from `res.json()` (e.g., non-JSON responses). An unhandled promise rejection will crash the function and leave the UI in a permanent loading state by not resetting `loading.value` to false.
- `/home/tarcisio/projects/singular/site-frontend/app/utils/formatDate.ts:30` – The use of `date.toISOString().split('T')[0]` is susceptible to timezone conversion errors. `toISOString()` converts the date to UTC, which can shift the date to the previous day for users in timezones ahead of UTC. This leads to incorrect date values for SEO and accessibility purposes.
- `/home/tarcisio/projects/singular/site-frontend/app/utils/getImgUrl.ts:1` – The use of `import.meta.glob` with `eager: true` for all images and icons forces every asset to be included in the initial JavaScript bundle. This can significantly increase the bundle size and slow down the initial page load, negatively impacting Core Web Vitals. Consider using dynamic imports (`eager: false`) or letting the build tool handle asset URLs directly.

## 🟡 Medium Severity
- `/home/tarcisio/projects/singular/site-frontend/app/components/BlogArticle/BlogArticle.vue:10` – The `id` attribute of the `h3` tag is dynamically bound to `blogArticle.title`. HTML `id` attributes must be unique and cannot contain spaces or certain special characters, which are often present in titles. This creates invalid HTML, which can break accessibility features (like `aria-labelledby` on the parent section) and CSS/JavaScript selectors targeting the id. The `blogArticle.slug` property, which is already available, should be used for the `id` instead.
- `/home/tarcisio/projects/singular/site-frontend/app/components/BlogArticle/models.ts:11` – The `BlogArticleType` interface includes both `link` and `slug` properties. This data model is redundant and can lead to inconsistencies if both are managed separately. The canonical URL for an article should be derived programmatically from the `slug` to ensure a single source of truth, reducing the risk of broken or incorrect links.
- `/home/tarcisio/projects/singular/site-frontend/app/components/BlogArticle/models.ts:15` – The `postDate` property in `BlogArticleType` is typed as `Date`. While using the `Date` object is a good practice for date manipulation, data fetched from an API typically provides dates as strings (e.g., ISO 8601 format). This implies a required data transformation step (parsing the string into a `Date` object). If this transformation is missed or fails, assigning a string to `postDate` can lead to runtime errors when `Date` methods are called (e.g., `TypeError: postDate.getFullYear is not a function`).
- `/home/tarcisio/projects/singular/site-frontend/app/components/Button/Button.vue:16` – The `<img>` tag for the icon is hardcoded with `aria-hidden="true"`, correctly marking it as decorative. However, the component also accepts and binds an `iconAlt` prop. For a decorative image that is hidden from assistive technologies, the `alt` attribute should be empty (`alt=""`). Providing `alt` text is contradictory to `aria-hidden="true"` and is an accessibility anti-pattern.
- `/home/tarcisio/projects/singular/site-frontend/app/components/Card/Card.vue:29` – The 'to' prop, which is passed to the Button component for navigation, is not validated. If the value for 'to' originates from an untrusted source (e.g., user-generated content or a third-party API), it could be exploited for Cross-Site Scripting (XSS) by using a 'javascript:' URL. The prop's value should be validated to ensure it points to a safe destination (e.g., starts with '/' or 'https://').
- `/home/tarcisio/projects/singular/site-frontend/app/components/Carousel/Carousel.vue:14` – Clickable navigation controls are implemented with `<p>` tags. For semantic correctness and accessibility, `<button>` elements should be used. This ensures they are keyboard-focusable and properly identified by assistive technologies.
- `/home/tarcisio/projects/singular/site-frontend/app/components/Carousel/Carousel.vue:35` – The scroll distance is hardcoded to 320px. This makes the component brittle and not reusable if the width of the carousel items changes or is responsive. The scroll amount should be calculated dynamically based on the slide or viewport width.
- `/home/tarcisio/projects/singular/site-frontend/app/components/Carousel/CarouselCard.vue:44` – The component manages its flipped state internally. This prevents a parent component from controlling or observing the card's state. For better reusability and component composition, consider using `defineModel()` or a prop/emit combination (`v-model`) to allow the parent to manage the flipped state.
- `/home/tarcisio/projects/singular/site-frontend/app/components/ConnectiCard/ConnectiCard.vue:12` – The custom `Button` component is used with props that suggest it will render a link opening in a new tab (`to` and `:new="true"`). If the underlying `<a>` tag is rendered with `target="_blank"` without `rel="noopener noreferrer"`, the application is vulnerable to tabnabbing, a security risk where the linked page can gain partial control over the originating page.
- `/home/tarcisio/projects/singular/site-frontend/app/components/Footer/Footer.vue:2` – The component unconditionally renders both `<FooterDesktop />` and `<FooterMobile />` simultaneously. This pattern, typically paired with CSS media queries to hide one, is suboptimal for performance in a Nuxt.js (SSR) application. Both components are included in the server-rendered HTML, increasing the initial page payload. On the client, both component instances are created, hydrated, and kept in memory, and any associated logic (e.g., API calls, computed properties) is executed, even for the hidden component. A better approach is to conditionally render only the necessary component using `v-if` based on a client-side viewport check to reduce payload size, client-side processing, and memory usage.
- `/home/tarcisio/projects/singular/site-frontend/app/components/Footer/FooterDesktop.vue:10` – The component contains a significant amount of hardcoded content, including navigation links, company details, addresses, and contact information. This makes the content difficult to update and maintain, as it requires code changes for any text modification. This data should be externalized, for example, to a central configuration file, a composable function, or fetched from a headless CMS to improve maintainability.
- `/home/tarcisio/projects/singular/site-frontend/app/components/Footer/FooterDesktop.vue:41` – Links using `target="_blank"` to open in a new tab are missing the `rel="noopener noreferrer"` attribute. This exposes the application to a 'tabnabbing' security vulnerability, where the newly opened page can gain malicious access to the originating page's `window` object.
- `/home/tarcisio/projects/singular/site-frontend/app/components/Footer/FooterMobile.vue:12` – The accordion structure using `<details>` and `<summary>` is duplicated four times. This repeated markup should be extracted into a reusable child component to reduce code duplication and improve the component's readability and maintainability.
- `/home/tarcisio/projects/singular/site-frontend/app/components/Footer/FooterMobile.vue:21` – All footer content, including navigation links, addresses, and contact information, is hardcoded directly in the template. This makes maintenance difficult and error-prone. This data should be managed in a centralized configuration or fetched dynamically to improve maintainability and reusability.
- `/home/tarcisio/projects/singular/site-frontend/app/components/Footer/FooterMobile.vue:101` – Social media links use `<a>` tags with an empty `href` attribute. This is not accessible and can cause the page to reload when clicked. These links should contain valid URLs or be replaced with non-interactive elements if they are placeholders.
- `/home/tarcisio/projects/singular/site-frontend/app/components/FranchiseComponent/FranchiseComponent.vue:28` – The 'Veja no Maps' and 'Vá de Waze' buttons are rendered even when `franchise.fullAddress` is empty. This results in links that open the map services without a destination, providing a poor user experience. The container for these buttons should be conditionally rendered with `v-if="franchise.fullAddress"` to ensure they only appear when a valid address is available.
- `/home/tarcisio/projects/singular/site-frontend/app/components/FranchiseComponent/models.ts:5` – The `phones` property is typed as an optional `string`. The plural name suggests there could be multiple phone numbers, but a single string is not a structured way to handle a list. This can lead to complex and error-prone string parsing logic in the component. Consider using `string[]` to represent a list of phone numbers or renaming the property to `phone` (singular) if only one is ever expected.
- `/home/tarcisio/projects/singular/site-frontend/app/components/FranchiseComponent/models.ts:6` – The `workingTime` property is a `string`, which forces the frontend to parse a potentially complex and inconsistent format (e.g., 'Mon-Fri: 9am-5pm', '24/7'). This is brittle and can lead to display bugs if the format from the data source changes. A more robust approach would be to use a structured object, such as `Array<{ day: string; hours: string; }>` or a similar format, to represent working hours, making the data easier and safer to consume.
- `/home/tarcisio/projects/singular/site-frontend/app/components/Header/DesktopMenu.vue:3` – The entire navigation structure is hardcoded directly in the template. This makes the menu difficult to maintain, update, or source from a headless CMS. The navigation data should be defined as a structured array or object in the `<script>` section and rendered using a `v-for` loop. This separates data from presentation, improving reusability and maintainability.
- `/home/tarcisio/projects/singular/site-frontend/app/components/Header/DesktopMenu.vue:5` – The dropdown menus appear to be implemented using CSS-only hover effects, as there is no associated script logic. This pattern is inaccessible to keyboard-only users who cannot hover, and it lacks the necessary ARIA attributes (like `aria-expanded`) for screen reader users. Interactive controls like menus should be implemented with JavaScript to handle click/keyboard events and manage ARIA states for proper accessibility.
- `/home/tarcisio/projects/singular/site-frontend/app/components/Header/DesktopMenu.vue:14` – External links opening in a new tab (`target="_blank"`) are missing the `rel="noopener noreferrer"` attribute. This creates a security vulnerability known as 'tabnabbing', where the newly opened page can gain partial control over the original page (`window.opener`). Always include `rel="noopener noreferrer"` on links with `target="_blank"` to mitigate this risk.
- `/home/tarcisio/projects/singular/site-frontend/app/components/Header/Header.vue:5` – Anchor tags for partner logos have an empty `href=""` attribute. Clicking these links will cause the current page to reload, which is likely unintended behavior. If they are not meant to be interactive links, they should be rendered with non-interactive elements like `<span>` or `<div>`. If they are links, they should point to a valid URL.
- `/home/tarcisio/projects/singular/site-frontend/app/components/Header/Header.vue:78` – The component manipulates the global `document.body.style.overflow` property but does not clean up this side effect when it's unmounted. If the mobile menu is open when the user navigates to another page, the `<body>` tag might remain locked (overflow: 'hidden'), making the new page unscrollable. An `onUnmounted` hook should be added to ensure the style is reset.
- `/home/tarcisio/projects/singular/site-frontend/app/components/Header/MobileMenu.vue:29` – The navigation menu items and their corresponding dropdowns are hardcoded in the template, leading to significant code duplication. This makes the component difficult to maintain and prone to errors. A data-driven approach, where menu items are defined in an array and rendered with `v-for`, would be significantly more scalable and maintainable.
- `/home/tarcisio/projects/singular/site-frontend/app/components/HealthPages/HealthPage.vue:21` – The `scrollTo('.form-container')` function targets an element that is currently commented out in the template. This will cause the button to fail silently and not perform the scroll action, leading to broken functionality.
- `/home/tarcisio/projects/singular/site-frontend/app/components/HealthPages/HealthPage.vue:65` – The line `history.replaceState(null, '', ' ');` in the `scrollTo` function is problematic. It modifies the browser's URL path to a single space, which can break page reloads, routing, and browser history. This is not a standard way to clear a URL hash and can have unintended side effects.
- `/home/tarcisio/projects/singular/site-frontend/app/components/HealthPages/models.ts:1` – The interfaces `BannerData`, `IntroductionData`, and `CarouselData` are not exported. This prevents them from being used to type the props of individual components that might consume these specific data structures (e.g., a `<Banner>` or `<Introduction>` component). To promote modularity and type-safety, constituent types that map to specific components or reusable data structures should be exported.
- `/home/tarcisio/projects/singular/site-frontend/app/components/MainBanner/MainBanner.vue:11` – The `section` prop is typed as a generic `Object`. This undermines the benefits of TypeScript by providing no type safety for the object's properties (`backgroundImage`, `title`, `description`, `destination`). A specific `interface` or `type` should be defined for the section's shape and used with Vue's `PropType` for better type checking, autocompletion, and code maintainability.
- `/home/tarcisio/projects/singular/site-frontend/app/components/MainBanner/MainBanner.vue:19` – The `document.querySelector` call can throw a `DOMException` if `props.section.destination` is not a valid CSS selector. This unhandled exception would crash the click event handler. It's safer to wrap this call in a `try...catch` block to handle potential invalid selector strings gracefully.
- `/home/tarcisio/projects/singular/site-frontend/app/components/RecentPosts/RecentPosts.vue:13` – The CSS class for column layout is based on the `limit` prop, while the number of rendered items is determined by `effectiveLimit`, which changes for mobile. This can lead to layout issues, such as a single item being placed in a three-column grid on small screens.
- `/home/tarcisio/projects/singular/site-frontend/app/components/RecentPosts/RecentPosts.vue:47` – The `stripHtml` function uses a regular expression that is insufficient for robust HTML sanitization. If the stripped content is ever rendered using `v-html` in a child component, this could introduce a Cross-Site Scripting (XSS) vulnerability. A dedicated sanitization library should be used for security.
- `/home/tarcisio/projects/singular/site-frontend/app/components/RecentPosts/RecentPosts.vue:54` – The key for `useAsyncData` is incomplete. It's missing `props.limit`, which is part of the query's dependencies and is included in the `watch` array. This can lead to caching bugs where changes to the `limit` prop do not result in a new cache entry, potentially serving stale data.
- `/home/tarcisio/projects/singular/site-frontend/app/components/RecentPosts/RecentPosts.vue:110` – The data transformation logic uses `post: any`, which negates the benefits of TypeScript. A proper type should be defined for the posts returned by the GraphQL query (`GqlRecentPosts`) to ensure type safety and improve code maintainability.
- `/home/tarcisio/projects/singular/site-frontend/app/components/SendPrescriptionCard/SendPrescriptionCard.vue:5` – The standard `<img>` tag is used instead of Nuxt's `<NuxtImg>` component. This misses out on powerful performance optimizations like automatic resizing, quality adjustments, and serving modern image formats (e.g., WebP, AVIF), which can significantly improve page load times.
- `/home/tarcisio/projects/singular/site-frontend/app/components/Testimonials/Testimonials.vue:19` – The call-to-action Button has a hardcoded destination (`to="/contato/orcamento"`) and slot content ('Faça o seu orçamento'). This tightly couples the Testimonials component to a specific navigation path, reducing its reusability. Consider passing the destination URL and button text as props or using a named slot for the button to make the component more flexible.
- `/home/tarcisio/projects/singular/site-frontend/app/components/TextsCarousel/Carousel.model.ts:4` – The property name 'dosis' appears to be a typo and was likely intended to be 'dose' or 'dosage'. Using non-standard or misspelled terms in a data model can lead to confusion, maintainability issues, and potential bugs when mapping data from an API or other sources that use the correct spelling.
- `/home/tarcisio/projects/singular/site-frontend/app/components/TextsCarousel/TextCarouselCard.vue:12` – The component imports styles via `import './styles.scss';` in the script setup block, which pollutes the global scope. This can lead to CSS conflicts and makes styles hard to manage. The recommended approach is to use a `<style lang="scss" scoped>` block to encapsulate styles.
- `/home/tarcisio/projects/singular/site-frontend/app/components/TextsCarousel/TextsCarousel.vue:83` – The `carouselTitleId` is generated using `Math.random()` inside a `computed` property. This causes the ID to be regenerated on every component re-render, which breaks the stable association required by `aria-labelledby`. The ID should be generated once and remain constant throughout the component's lifecycle, for instance by using a simple `const` variable defined in the `<script setup>` block.
- `/home/tarcisio/projects/singular/site-frontend/app/components/TextsCarousel/TextsCarousel.vue:133` – The 'resize' event listener on the `window` object calls `calculateMetrics` directly without debouncing or throttling. This can trigger excessive DOM measurements and reflows during a resize operation, causing performance degradation. A debounced or throttled handler should be used. Alternatively, consider using a `ResizeObserver` (e.g., via `vue-use`) to react to changes in the component's dimensions more efficiently.
- `/home/tarcisio/projects/singular/site-frontend/app/components/VideoPlayer.vue:40` – The `@click="togglePlay"` handler on the `<video>` element causes unintended behavior due to event bubbling. When a user clicks on child elements like the progress bar or volume slider, the click event bubbles up to the video container, triggering a play/pause action. This leads to a frustrating user experience where seeking or changing volume also pauses or plays the video. Event propagation from the controls should be stopped, for example by adding `@click.stop` to the controls' container.
- `/home/tarcisio/projects/singular/site-frontend/app/components/VideoPlayer.vue:150` – The `isPlaying` reactive state is toggled manually, which can become out of sync with the video element's actual playback state. The `play()` method is asynchronous and can be rejected by the browser (e.g., autoplay policies). This can lead to the UI showing a 'playing' state while the video is paused. It's more robust to update `isPlaying` based on the video element's `play` and `pause` events.
- `/home/tarcisio/projects/singular/site-frontend/app/components/contact/ContactCard.vue:3` – The component contains hardcoded, repetitive structures for each contact method. This violates the DRY (Don't Repeat Yourself) principle and makes the component inflexible and difficult to maintain. Consider refactoring to accept an array of contact items as a prop and render them using a `v-for` loop.
- `/home/tarcisio/projects/singular/site-frontend/app/components/contact/Form.vue:167` – The component manages a `loading` state but lacks a dedicated mechanism for handling and displaying submission errors. A truly reusable form component should typically accept an `error` prop or provide a slot for error messages, allowing the parent component to communicate submission failures back to the user. Without this, the component is less robust and provides a poorer user experience upon failure.
- `/home/tarcisio/projects/singular/site-frontend/app/components/contact/SubscribeForm/SubscribeForm.vue:5` – The native `<img>` tag is used instead of Nuxt's `<NuxtImg>` component. This misses out on critical performance optimizations such as automatic responsive sizing, modern format conversion (e.g., WebP/AVIF), and lazy-loading.
- `/home/tarcisio/projects/singular/site-frontend/app/pages/agenda-de-eventos/index.vue:167` – The `scrollTo` function is defined but is never called, referenced, or passed to a child component. This is unused code (dead code) and should be removed if it serves no purpose.
- `/home/tarcisio/projects/singular/site-frontend/app/pages/blog/[slug].vue:95` – The code uses a non-null assertion `post.categories[0]!` when passing the category slug to the `RecentPosts` component. This will cause a runtime error if the `post.categories` array is empty, as `post.categories[0]` would be `undefined`. A check should be added to ensure the array is not empty before accessing its first element.
- `/home/tarcisio/projects/singular/site-frontend/app/pages/blog/[slug].vue:155` – The post date is created with `new Date(fetchedPost.date ?? '')`. If `fetchedPost.date` is null or undefined, this results in `new Date('')`, which is an 'Invalid Date' object. The `formatDisplayDate` function will then throw a `RangeError` when `toLocaleDateString` is called on it. The case of a null or undefined date should be handled gracefully.
- `/home/tarcisio/projects/singular/site-frontend/app/pages/blog/index.vue:200` – The application fetches all blog posts and then performs filtering and pagination on the client-side. This approach does not scale well and can lead to performance degradation, increased memory consumption, and slower initial load times as the number of posts grows. Filtering and pagination should be handled by the backend API.
- `/home/tarcisio/projects/singular/site-frontend/app/pages/blog/index.vue:250` – Direct DOM manipulation using `document.querySelector` is a Vue anti-pattern that couples logic tightly to the DOM structure. Instead, use template refs (`ref()`) to get a reference to the element and call `scrollIntoView` on the ref's value. This makes the component more declarative and maintainable.
- `/home/tarcisio/projects/singular/site-frontend/app/pages/contato/orcamento.vue:39` – The `form` parameter is typed as `any`. This bypasses TypeScript's type-checking benefits, reducing code safety and maintainability. A specific interface or type should be defined for the form data structure to ensure properties like `fullName`, `email`, `attachment`, etc., exist and are of the correct type.
- `/home/tarcisio/projects/singular/site-frontend/app/pages/contato/orcamento.vue:54` – The native `fetch` API is used instead of Nuxt's built-in `$fetch` utility. Using `$fetch` is the idiomatic approach in Nuxt 3 as it provides benefits like automatic JSON parsing, throwing errors on non-2xx responses, and better integration with the Nuxt ecosystem.
- `/home/tarcisio/projects/singular/site-frontend/app/pages/contato/orcamento.vue:60` – The code does not check the HTTP response status before attempting to parse the JSON body. If the server responds with an error status (e.g., 400, 500), the logic proceeds as if it were a success, which can lead to unexpected behavior or errors. Check `res.ok` before calling `res.json()`.
- `/home/tarcisio/projects/singular/site-frontend/app/pages/contato/trabalhe-conosco.vue:48` – The 'form' parameter in the 'submitCurriculum' function is typed as 'any'. This defeats the purpose of TypeScript by sacrificing type safety and autocompletion. A specific interface or type should be defined for the form's data structure.
- `/home/tarcisio/projects/singular/site-frontend/app/pages/contato/trabalhe-conosco.vue:61` – The API endpoint URL is hardcoded. This is poor practice for maintainability and deployment across different environments (development, staging, production). The URL should be sourced from Nuxt's runtime configuration or environment variables.
- `/home/tarcisio/projects/singular/site-frontend/app/pages/dermatologia/index.vue:13` – The 'ogImage' and 'twitterImage' properties for SEO meta tags are being set with an empty string from 'seo.image'. This will cause missing or broken image previews when the page URL is shared on social media platforms like Facebook, X (Twitter), and LinkedIn, negatively impacting marketing efforts.
- `/home/tarcisio/projects/singular/site-frontend/app/pages/formas-farmaceuticas/index.vue:123` – The page is configured with a robots meta tag set to `noindex,nofollow`. This prevents search engines from indexing the page and following its links, which is highly detrimental to SEO for what appears to be a primary content page. This configuration should be removed unless it is intentionally set for a non-production or private page.
- `/home/tarcisio/projects/singular/site-frontend/app/pages/index.vue:70` – The `testimonials` data array, defined starting on line 188, is never used. The `<Testimonials>` component on line 70 is rendered without this data, indicating either dead code or an incomplete feature implementation. The component likely expects this data to be passed as a prop.
- `/home/tarcisio/projects/singular/site-frontend/app/pages/index.vue:104` – The page's robot meta tag is hardcoded to 'noindex,nofollow' on line 104. This will prevent search engines from indexing the page, which is highly undesirable for a production homepage. This setting should be environment-dependent, allowing indexing in production.
- `/home/tarcisio/projects/singular/site-frontend/app/pages/longevidade/index.vue:13` – The 'seo.image' property is an empty string. This results in missing 'og:image' and 'twitter:image' meta tags, preventing a preview image from being displayed when the page URL is shared on social media platforms. This negatively impacts click-through rates and user engagement.
- `/home/tarcisio/projects/singular/site-frontend/app/pages/martins-brasil/index.vue:51` – The `form` parameter in the `submitMartinsBrasil` function is typed as `any`. This bypasses TypeScript's type safety, making it possible for runtime errors to occur if the shape of the emitted form data changes. A specific interface or type should be defined for the form payload.
- `/home/tarcisio/projects/singular/site-frontend/app/pages/martins-brasil/index.vue:58` – The backend API endpoint is hardcoded in the component. In a Nuxt application, it is best practice to proxy such requests through server API routes (`/server/api`). This avoids exposing the backend URL directly to the client, helps manage CORS, and allows for the use of environment variables for different deployment stages.
- `/home/tarcisio/projects/singular/site-frontend/app/pages/nossa-historia/index.vue:48` – The `<img>` tags lack `width` and `height` attributes. This can cause Cumulative Layout Shift (CLS), a key Core Web Vitals metric, as the browser cannot reserve space for the images before they load. This leads to a poor user experience.
- `/home/tarcisio/projects/singular/site-frontend/app/pages/nossa-historia/index.vue:107` – The 'og:image' and 'twitter:image' properties for SEO are set to an empty string. This will result in missing or poorly generated preview images when the page is shared on social media platforms, negatively impacting engagement.
- `/home/tarcisio/projects/singular/site-frontend/app/pages/nossas-unidades/index.vue:4` – The `v-for` directives on lines 4 and 7 are missing the `:key` attribute. A unique key (e.g., `:key="franchise.title"`) is crucial for Vue to track each node's identity, preventing potential rendering bugs and improving performance, especially with dynamic lists.
- `/home/tarcisio/projects/singular/site-frontend/app/pages/nossas-unidades/index.vue:40` – The SEO image for social sharing (`ogImage` and `twitterImage`) is an empty string. This will result in poor or missing link previews when the page is shared on social media platforms, negatively impacting user engagement.
- `/home/tarcisio/projects/singular/site-frontend/app/pages/nossas-unidades/index.vue:68` – The franchise data (`mainFranchises` and `gridFranchises`) is hardcoded directly within the component. This makes the component difficult to maintain and violates the separation of concerns. This data should be fetched from a CMS/API or at least externalized into a separate data file (e.g., in a `/server` or `/content` directory) to be consumed by the page.
- `/home/tarcisio/projects/singular/site-frontend/app/pages/nutricao/index.vue:14` – The `seo.image` property is initialized as an empty string and used directly for the `ogImage` and `twitterImage` meta tags. This is invalid and will result in missing preview images when the page link is shared on social media platforms. A valid, absolute URL to an image should be provided.
- `/home/tarcisio/projects/singular/site-frontend/app/pages/saude-da-mulher/index.vue:12` – The 'seo.image' constant is an empty string, which is then used for 'ogImage' and 'twitterImage'. This will result in no preview image when the page is shared on social media, negatively impacting engagement and brand presentation.
- `/home/tarcisio/projects/singular/site-frontend/app/pages/saude-da-mulher/index.vue:56` – The Call To Action (CTA) link in the introduction section is an empty string. This will render a non-functional button or a link that reloads the page, leading to a poor user experience.
- `/home/tarcisio/projects/singular/site-frontend/app/pages/saude-da-mulher/index.vue:81` – The description for the form section contains a content error. On a page about 'Saúde da Mulher' (Women's Health), it incorrectly refers to 'saúde masculina' (men's health). This appears to be a copy-paste error that can confuse users.
- `/home/tarcisio/projects/singular/site-frontend/app/pages/saude-do-homem/index.vue:13` – The `seo.image` property is an empty string, which results in empty 'og:image' and 'twitter:image' meta tags. This prevents a preview image from being displayed when the page link is shared on social media or messaging platforms, potentially reducing user engagement and click-through rates.
- `/home/tarcisio/projects/singular/site-frontend/app/pages/saude-intestinal/index.vue:13` – The 'seo.image' property is an empty string. This results in missing 'og:image' and 'twitter:image' meta tags, which will cause poor display and lack of a preview image when the URL is shared on social media platforms.
- `/home/tarcisio/projects/singular/site-frontend/app/pages/saude-intestinal/index.vue:40` – The entire page content is hardcoded as a large TypeScript object ('intestinalHealthData'). This approach makes content updates cumbersome, as they require code changes and a new deployment. For better maintainability, this content should be fetched from a headless CMS or a dedicated API.
- `/home/tarcisio/projects/singular/site-frontend/app/pages/seja-um-franqueado/index.vue:135` – The `form` parameter in the `submitFranchise` function is typed as `any`. This bypasses TypeScript's static type checking, reducing code safety and maintainability. A specific interface should be defined for the form data to ensure type safety.
- `/home/tarcisio/projects/singular/site-frontend/app/pages/seja-um-franqueado/index.vue:142` – The backend API endpoint URL is hardcoded within the component. This makes managing different environments (e.g., development, staging, production) difficult and error-prone. The URL should be provided through Nuxt's runtime configuration or environment variables.
- `/home/tarcisio/projects/singular/site-frontend/app/utils/formatDate.ts:9` – The function does not handle cases where `dateInput` is an unparsable string (e.g., 'invalid-date'). This creates an 'Invalid Date' object, and the subsequent call to `Intl.DateTimeFormat(...).format(date)` will throw a `RangeError`, causing an unhandled application crash.
- `/home/tarcisio/projects/singular/site-frontend/app/utils/formatDate.ts:28` – The function does not handle cases where `dateInput` is an unparsable string. This creates an 'Invalid Date' object, and the subsequent call to `date.toISOString()` will throw a `RangeError`, causing an unhandled application crash.
- `/home/tarcisio/projects/singular/site-frontend/app/utils/getImgUrl.ts:9` – The function constructs asset paths by directly appending extensions to the input `path`. If the provided `path` string already includes a file extension (e.g., 'logo.png'), this will result in an incorrect path like '../assets/images/logo.png.webp', which will fail to resolve the asset. The function should be more robust, for example by stripping any existing extension from the input `path`.
- `/home/tarcisio/projects/singular/site-frontend/app/utils/getImgUrl.ts:11` – The function returns an empty string (`''`) when an asset is not found. When an empty string is used as the `src` for an `<img>` tag, the browser makes a request to the current page's URL, which is inefficient and can lead to unexpected 404 errors or server load. It is better to return `null` or `undefined` to allow the calling component to conditionally render the image element.
- `/home/tarcisio/projects/singular/site-frontend/app/utils/slugify.ts:21` – The function can produce slugs with leading or trailing hyphens. For example, an input like '- My Title -' would result in '-my-title-'. URL slugs should not typically start or end with separators. This can be resolved by chaining an additional `.replace(/^-+|-+$/g, '')` at the end to trim any leading or trailing hyphens.
- `/home/tarcisio/projects/singular/site-frontend/app/utils/types.ts:14` – The `CarouselItem` and `SectionType` types lack a unique identifier field like `id`. When rendering a list of such items in Vue using `v-for`, the absence of a unique key can lead to inefficient DOM updates and potential state management issues, especially if the list is dynamic. It is a best practice for all list entities to have a unique identifier.

## 🔵 Low Severity
- `/home/tarcisio/projects/singular/site-frontend/app/components/BlogArticle/BlogArticle.vue:31` – The `withDefaults` factory function computes a `defaultSlug` by calling `slugify`. While this is functionally correct, this computation runs every time a component instance is created without the `blogArticle` prop. For a static default value, it's more performant to pre-compute the slug and define it as a constant outside the component setup, making the default factory function only return a static object.
- `/home/tarcisio/projects/singular/site-frontend/app/components/BlogArticle/BlogArticle.vue:50` – The `resolvedImgUrl` computed property returns an empty string if `props.blogArticle.imgUrl` is empty. This results in an `<img>` tag with `src=""`, which causes the browser to make a request to the current page's URL. This is an unnecessary network request and can add noise to network logs. It would be better to return a path to a default placeholder image or `undefined` to avoid rendering the `src` attribute.
- `/home/tarcisio/projects/singular/site-frontend/app/components/BlogArticle/models.ts:13` – In `BlogArticleType`, the `categories` property is typed as `string[]`. This is ambiguous as it is unclear whether the strings represent category names, IDs, or slugs. For improved type safety and code clarity, consider using a more specific type, such as `CategoryType['slug'][]` or `CategoryType[]` if the full category objects are needed. This makes the relationship between articles and categories explicit and less error-prone.
- `/home/tarcisio/projects/singular/site-frontend/app/components/Button/Button.vue:44` – The prop name 'new' is a reserved keyword in JavaScript. While this is valid syntax for a Vue prop, it is not idiomatic and can lead to developer confusion or errors if the props are destructured in `<script>` logic. A more descriptive name like 'openInNewTab' would be clearer and safer.
- `/home/tarcisio/projects/singular/site-frontend/app/components/Card/Card.vue:4` – The function call 'getImageUrl(logo, true)' uses a 'magic boolean' as its second argument. This makes the code harder to understand and maintain as the purpose of 'true' is not immediately clear. Consider refactoring 'getImageUrl' to accept a more descriptive options object (e.g., '{ type: "logo" }') or at least add a comment explaining the parameter.
- `/home/tarcisio/projects/singular/site-frontend/app/components/Card/Card.vue:4` – The 'getImageUrl' function is called directly in the template. Functions called in template expressions are re-evaluated on every render cycle of the component. If this function were computationally expensive, it could lead to a performance bottleneck. For better performance and caching, its result should be wrapped in a 'computed' property.
- `/home/tarcisio/projects/singular/site-frontend/app/components/Carousel/Carousel.vue:14` – The navigation buttons use symbolic characters ('‹', '›') but lack descriptive text or `aria-label` attributes. This is poor for accessibility as screen readers may not announce their function clearly. An `aria-label` such as 'Previous items' should be added.
- `/home/tarcisio/projects/singular/site-frontend/app/components/Carousel/Carousel.vue:21` – The `items` prop is defined using the generic `Array` constructor. In a TypeScript project, it's idiomatic to use `defineProps<...>()` with a specific interface (e.g., `interface Item { id: number; ... }`) to provide strong type safety for the items within the array.
- `/home/tarcisio/projects/singular/site-frontend/app/components/Carousel/CarouselCard.vue:46` – The `CardItem` interface is defined locally within the component. If this type is used by the parent component or in API definitions, it should be extracted to a shared types file (e.g., `types/index.ts`) to maintain a single source of truth and improve code organization.
- `/home/tarcisio/projects/singular/site-frontend/app/components/Carousel/CarouselCard.vue:48` – The `backgroundImage` property in the `CardItem` interface is typed as `string`, but the template logic (`v-if="item.backgroundImage"`) implies it can be optional or falsy. The type should be more explicit, such as `backgroundImage?: string` or `backgroundImage: string | null`, to accurately reflect its potential values.
- `/home/tarcisio/projects/singular/site-frontend/app/components/ConnectiCard/ConnectiCard.vue:4` – The image source uses a relative path (`../../assets/images/connecti.png`). In a Nuxt.js project, it is best practice to use the `~/assets` alias (`~/assets/images/connecti.png`). This makes the component more portable and less prone to breaking if its location in the directory tree changes.
- `/home/tarcisio/projects/singular/site-frontend/app/components/ConnectiCard/ConnectiCard.vue:4` – The image file (`connecti.png`) is referenced directly. For performance optimization, ensure the image is compressed and served in an optimal format (like WebP or AVIF). Large, unoptimized images can significantly slow down page load and negatively impact Core Web Vitals.
- `/home/tarcisio/projects/singular/site-frontend/app/components/ConnectiCard/ConnectiCard.vue:17` – Styles are imported directly within the `<script>` block. The more idiomatic and common convention in Vue Single File Components is to use a `<style>` block, either by linking to the file (`<style lang="scss" scoped src="./styles.scss"></style>`) or embedding the styles directly. This better leverages Vue's SFC tooling and improves code organization.
- `/home/tarcisio/projects/singular/site-frontend/app/components/Footer/Footer.vue:2` – Having both desktop and mobile footer components present in the DOM at the same time can cause accessibility issues. Even if one is visually hidden with `display: none`, some screen readers might still parse its content, leading to duplicate links and information being announced. This creates a confusing experience for users of assistive technologies. Conditionally rendering only one version of the footer ensures a clean and unambiguous accessibility tree.
- `/home/tarcisio/projects/singular/site-frontend/app/components/Footer/FooterDesktop.vue:1` – The component architecture suggested by the filename `FooterDesktop.vue` implies a separate component exists for mobile views. This pattern often leads to code duplication and increased maintenance overhead. A better practice is to create a single, responsive `Footer.vue` component that adapts its layout using CSS media queries, ensuring a single source of truth for the footer's content and structure.
- `/home/tarcisio/projects/singular/site-frontend/app/components/Footer/FooterDesktop.vue:6` – The component uses standard `<img>` tags for all images. This misses the opportunity for significant performance optimizations. Using Nuxt's built-in `<NuxtImg>` component would enable automatic responsive resizing, modern format conversion (e.g., WebP), and lazy loading, which can substantially improve page load times and Core Web Vitals.
- `/home/tarcisio/projects/singular/site-frontend/app/components/Footer/FooterDesktop.vue:26` – Some internal links use relative paths (e.g., `href="nossa-historia"`) instead of root-relative paths (`href="/nossa-historia"`). This can lead to broken links if the component is rendered on a nested route (e.g., `/category/page`). All internal links should be consistently defined with root-relative paths to ensure they resolve correctly from anywhere in the application.
- `/home/tarcisio/projects/singular/site-frontend/app/components/Footer/FooterMobile.vue:6` – The component uses relative paths (`../../assets/...`) to reference static assets. This is less maintainable than using path aliases (e.g., `~/assets/...`) which are provided by Nuxt and are resilient to file location changes.
- `/home/tarcisio/projects/singular/site-frontend/app/components/Footer/FooterMobile.vue:39` – A `href` attribute on line 39 is missing a leading slash for a root-relative link ('nossa-historia'), which can lead to incorrect navigation depending on the current URL path. It should be '/nossa-historia'.
- `/home/tarcisio/projects/singular/site-frontend/app/components/FranchiseComponent/FranchiseComponent.vue:1` – The component filename `FranchiseComponent.vue` contains a redundant 'Component' suffix. A common Vue/Nuxt convention is to name component files based on their specific purpose, such as `FranchiseCard.vue`. This improves clarity and aligns better with community best practices, especially given the root element's class is 'franchise-card'.
- `/home/tarcisio/projects/singular/site-frontend/app/components/FranchiseComponent/FranchiseComponent.vue:16` – The `tel:` link for the phone number is constructed using the raw `franchise.phones` string. This string may contain formatting (e.g., spaces, parentheses) that is not robust for a URI. The value for the `href` attribute should be a sanitized version of the phone number (e.g., by removing all non-digit characters) to ensure maximum compatibility across devices.
- `/home/tarcisio/projects/singular/site-frontend/app/components/FranchiseComponent/models.ts:1` – The interface name `FranchiseType` includes a 'Type' suffix. A more idiomatic TypeScript convention is to name interfaces without such suffixes (e.g., `Franchise`). This improves code readability and aligns with common community practices.
- `/home/tarcisio/projects/singular/site-frontend/app/components/FranchiseTestimonials/FranchiseTestimonials.vue:2` – The component is tightly coupled with its data source by directly importing from '@/data/franchise-testimonials'. This limits its reusability for other sets of testimonials. A better practice is to make the component presentational by accepting the testimonials array as a prop. This would allow it to be reused with different data sources throughout the application.
- `/home/tarcisio/projects/singular/site-frontend/app/components/FranchiseTestimonials/FranchiseTestimonials.vue:2` – Static data imports are included in the component's JavaScript bundle. If the `franchise-testimonials` data set becomes large, it could negatively impact the initial page load time. For larger static datasets, consider fetching the data on demand or leveraging Nuxt's server directory to serve it as an API endpoint.
- `/home/tarcisio/projects/singular/site-frontend/app/components/Header/Header.vue:6` – Static asset paths use relative directory traversal (`../../assets/...`). In a Nuxt application, it is best practice to use the `~/assets` alias. This makes the paths more robust and less likely to break if the component file is moved.
- `/home/tarcisio/projects/singular/site-frontend/app/components/Header/Header.vue:78` – The `watch` function is unnecessarily placed inside the `onMounted` lifecycle hook. In Vue 3's Composition API, watchers can be declared directly in the `setup` function body. They are automatically tied to the component's lifecycle and cleaned up on unmount. This is the idiomatic and more readable approach.
- `/home/tarcisio/projects/singular/site-frontend/app/components/Header/MobileMenu.vue:187` – The component uses a custom `<Button to="...">` component for navigation. This component should be implemented to internally use `<NuxtLink>` for internal routes to ensure client-side navigation. If it renders a standard `<a>` tag, it will contribute to unnecessary full page reloads.
- `/home/tarcisio/projects/singular/site-frontend/app/components/Header/MobileMenu.vue:218` – The logic for toggling dropdown visibility relies on hardcoded 'magic strings' (e.g., 'sobre', 'solucoes'). This is repeated for each menu item, increasing the risk of typos and making the code harder to refactor. This issue would be resolved by adopting a data-driven approach for the menu.
- `/home/tarcisio/projects/singular/site-frontend/app/components/HealthPages/HealthPage.vue:19` – Using the `index` as a key in `v-for` is an anti-pattern when the list can change. While acceptable for a static list, it's a poor practice that can lead to bugs and performance issues with dynamic lists. A unique identifier from the `paragraph` object itself should be preferred.
- `/home/tarcisio/projects/singular/site-frontend/app/components/HealthPages/HealthPage.vue:46` – The `RecentPosts` and `Form` components are asynchronously imported but the template code that uses them is commented out. This results in unused variables and dead code, which should be removed if not intended for future use.
- `/home/tarcisio/projects/singular/site-frontend/app/components/HealthPages/models.ts:1` – Interface names include a `Data` suffix (e.g., `BannerData`). In TypeScript, it's generally idiomatic to name interfaces without such suffixes (e.g., `Banner`, `Introduction`), as the fact that they define the shape of data is implicit. This leads to cleaner and more concise code.
- `/home/tarcisio/projects/singular/site-frontend/app/components/HealthPages/models.ts:9` – The `cta` and `image` objects within `IntroductionData` represent common, reusable patterns. Consider extracting them into their own shared interfaces (e.g., `CTA`, `Image`) to promote reusability and consistency across different data models in the application.
- `/home/tarcisio/projects/singular/site-frontend/app/components/HealthPages/models.ts:26` – The `cards` property within the `CarouselData` interface uses an inline object definition for its array elements. It is better practice to extract this into its own named interface, such as `CarouselCard`. This improves readability, allows the card type to be reused elsewhere, and simplifies prop typing for a potential `<CarouselCard>` component.
- `/home/tarcisio/projects/singular/site-frontend/app/components/HealthPages/models.ts:29` – The property name `dosis` within the `CarouselData.cards` object appears to be a potential typo for 'dose' or 'dosage'. While this may be intentional based on the data source, it's an unconventional term that could lead to confusion for other developers or mismatches with API contracts.
- `/home/tarcisio/projects/singular/site-frontend/app/components/MainBanner/MainBanner.vue:18` – A `console.log` statement is present in the `scrollTo` function. This appears to be leftover debugging code and should be removed from the production build to keep the browser console clean.
- `/home/tarcisio/projects/singular/site-frontend/app/components/RecentPosts/RecentPosts.vue:47` – The `stripHtml` function is a generic utility defined within the component's scope. It should be extracted to a shared utility file (e.g., `~/utils/html.ts`) to promote reusability and separate concerns.
- `/home/tarcisio/projects/singular/site-frontend/app/components/RecentPosts/RecentPosts.vue:73` – The responsive logic using `window.matchMedia` and lifecycle hooks is boilerplate. This logic should be encapsulated into a reusable composable (e.g., `useMediaQuery`) to clean up the component script and make the logic reusable across the application.
- `/home/tarcisio/projects/singular/site-frontend/app/components/RecentPosts/RecentPosts.vue:125` – On mobile devices, the component fetches a number of posts based on the original `limit` prop but then displays fewer items based on `effectiveLimit`. This results in over-fetching and processing data that is not used, which is a minor performance bottleneck.
- `/home/tarcisio/projects/singular/site-frontend/app/components/SendPrescriptionCard/SendPrescriptionCard.vue:5` – The `alt` attribute on the `<img>` tag is empty. For accessibility, if the image conveys information, it should have a descriptive alt text. If the image is purely decorative, `alt=""` is correct, but this should be an explicit decision rather than an omission.
- `/home/tarcisio/projects/singular/site-frontend/app/components/SendPrescriptionCard/SendPrescriptionCard.vue:5` – The image `src` uses a relative path (`../../assets/...`). The idiomatic Nuxt approach is to use the `~/assets` alias (`~/assets/images/...`). This makes the component's path resolution independent of its location in the directory tree, improving maintainability.
- `/home/tarcisio/projects/singular/site-frontend/app/components/SendPrescriptionCard/SendPrescriptionCard.vue:8` – Text content is hardcoded directly in the template. This makes the component less reusable and difficult to internationalize (i18n). Consider passing text as props or using a centralized content management or i18n solution.
- `/home/tarcisio/projects/singular/site-frontend/app/components/SendPrescriptionCard/SendPrescriptionCard.vue:9` – The navigation path '/contato/orcamento' is hardcoded in the `to` prop. This is brittle and can lead to broken links if routes are refactored. It's best practice in Nuxt to use named routes (e.g., `:to="{ name: 'contato-orcamento' }"`) for more robust and maintainable routing.
- `/home/tarcisio/projects/singular/site-frontend/app/components/SendPrescriptionCard/SendPrescriptionCard.vue:14` – Styles are imported within the `<script setup>` block. The conventional approach in Vue SFCs is to use a `<style>` block (e.g., `<style lang="scss" src="./styles.scss"></style>`) to handle styles, which provides better separation of concerns and integration with Vue-specific tooling.
- `/home/tarcisio/projects/singular/site-frontend/app/components/Testimonials/Testimonials.vue:2` – The component statically imports testimonial data directly. This includes the data in the initial JavaScript bundle, which can increase its size. For larger datasets, it is more performant to fetch data asynchronously using Nuxt's `useAsyncData` or `useFetch` to avoid bloating the main bundle.
- `/home/tarcisio/projects/singular/site-frontend/app/components/Testimonials/Testimonials.vue:19` – The `Button` component is explicitly passed `:new="false"`. If the default value for the `new` prop within the Button component is already `false`, this binding is redundant. Relying on default prop values leads to cleaner and more concise component usage.
- `/home/tarcisio/projects/singular/site-frontend/app/components/TextsCarousel/TextCarouselCard.vue:7` – The text label 'Dose usual:' is hardcoded directly in the template. This hinders internationalization (i18n) efforts and makes text management less flexible. Consider using a translation library or a centralized constants file for UI strings.
- `/home/tarcisio/projects/singular/site-frontend/app/components/TextsCarousel/TextCarouselCard.vue:14` – The 'card' prop is defined without runtime validators or default values. While TypeScript provides static type safety, this offers no protection against invalid data passed at runtime (e.g., from an API). Adding a `v-if` guard in the template or using `withDefaults` for optional props would make the component more robust.
- `/home/tarcisio/projects/singular/site-frontend/app/components/TextsCarousel/TextsCarousel.vue:49` – The `alt` attribute for the `<img>` in the 'next' slide button is 'Seta para a esquerda', which is incorrect and misleading for screen reader users. The alt text should accurately describe the button's function, such as 'Seta para a direita'.
- `/home/tarcisio/projects/singular/site-frontend/app/components/TextsCarousel/TextsCarousel.vue:69` – The import path for `TextCarouselCard` is './TextCarouselCard.vue/TextCarouselCard.vue'. This is an unconventional and potentially incorrect path structure. It may indicate a typo or a misconfigured project setup, which can lead to maintenance difficulties and confusion. The path should likely be './TextCarouselCard.vue'.
- `/home/tarcisio/projects/singular/site-frontend/app/components/TextsCarousel/TextsCarousel.vue:109` – The carousel's movement logic assumes all slides have the same width by measuring only the first slide (`firstSlide.offsetWidth`). If the `cards` prop contains items that render with different widths, the carousel's offset calculation will be incorrect, leading to visual bugs. This makes the component brittle and prone to errors if the content changes.
- `/home/tarcisio/projects/singular/site-frontend/app/components/VideoPlayer.vue:39` – The `updateProgress` method is bound to the `timeupdate` event, which can fire several times per second during video playback. This causes frequent updates to the `progress` ref and re-rendering of the progress bar. On lower-end devices, this could lead to minor performance degradation. Throttling the event handler is a common optimization to reduce the frequency of these updates without a noticeable impact on UX.
- `/home/tarcisio/projects/singular/site-frontend/app/components/VideoPlayer.vue:42` – The video player controls are missing a visible text display for the current time and total duration (e.g., '01:23 / 05:40'). This is a standard user experience and accessibility feature for video players. Relying solely on a visual progress bar is insufficient for users to accurately gauge their position in the video.
- `/home/tarcisio/projects/singular/site-frontend/app/components/VideoPlayer.vue:183` – The `toggleFullscreen` function contains highly repetitive code for handling vendor prefixes for the Fullscreen API. This reduces readability and maintainability. This logic could be abstracted into a separate utility function or simplified by using a small, dedicated library to handle cross-browser fullscreen functionality.
- `/home/tarcisio/projects/singular/site-frontend/app/components/VideoPlayer.vue:188` – The code uses `as any` type casting to access vendor-prefixed Fullscreen API methods (e.g., `mozRequestFullScreen`, `webkitRequestFullscreen`). This bypasses TypeScript's type-checking benefits. A safer approach is to augment the global `HTMLElement` and `Document` interfaces with these non-standard properties to maintain type safety throughout the application.
- `/home/tarcisio/projects/singular/site-frontend/app/components/contact/Contact.model.ts:4` – All string properties are defined as required. If any of these form fields are optional (e.g., 'phone'), they should be marked as optional in the interface (e.g., `phone?: string;` or `phone: string | null;`) to accurately model the data state and improve type safety.
- `/home/tarcisio/projects/singular/site-frontend/app/components/contact/Contact.model.ts:8` – The property `attachment` is named in the singular, but its type `File[]` suggests it can hold multiple files. To improve clarity and prevent potential confusion, consider renaming it to `attachments` to match its array type.
- `/home/tarcisio/projects/singular/site-frontend/app/components/contact/ContactCard.vue:5` – Image `src` attributes use relative paths (`../../assets/...`). In Nuxt.js, it is idiomatic and more robust to use the `~/assets` alias (e.g., `~/assets/icons/phone.svg`). This ensures correct path resolution and allows the build tool to process the assets.
- `/home/tarcisio/projects/singular/site-frontend/app/components/contact/ContactCard.vue:31` – The external link to WhatsApp is missing the `rel="noopener noreferrer"` attribute. This is a security best practice to prevent potential tabnabbing vulnerabilities.
- `/home/tarcisio/projects/singular/site-frontend/app/components/contact/ContactCard.vue:36` – The stylesheet is imported directly within the `<script>` block. The idiomatic approach in Vue is to use a `<style>` block (e.g., `<style lang="scss" scoped src="./ContactCard.scss"></style>`), which provides benefits like style scoping to prevent CSS conflicts.
- `/home/tarcisio/projects/singular/site-frontend/app/components/contact/Form.vue:228` – The `fileLabelText` computed property uses a type assertion (`props.variant as keyof typeof labels`). While it works in the current context, this creates a brittle coupling between the prop's possible values and the keys of the `labels` object. If the `variant` prop is ever updated without a corresponding change to the `labels` object, it could result in an `undefined` value at runtime without a compile-time error. A more type-safe mapping would be preferable.
- `/home/tarcisio/projects/singular/site-frontend/app/components/contact/Form.vue:242` – The `submitButtonText` computed property defines a text for a `'franchise'` variant. However, `'franchise'` is not included in the `variant` prop's validator, making the line `'franchise': 'Enviar formulário'` unreachable dead code.
- `/home/tarcisio/projects/singular/site-frontend/app/components/contact/SubscribeForm/SubscribeForm.vue:5` – The `<img>` tag has an empty `alt` attribute. This should be verified as intentionally decorative. If the image provides context, it requires descriptive alt text for accessibility compliance.
- `/home/tarcisio/projects/singular/site-frontend/app/components/contact/SubscribeForm/SubscribeForm.vue:8` – The component contains hardcoded text content and asset paths. For better reusability and maintainability (e.g., for internationalization), this data should be supplied via props.
- `/home/tarcisio/projects/singular/site-frontend/app/pages/agenda-de-eventos/index.vue:3` – The year '2026' is hardcoded in the `h2` heading. This creates a maintenance issue, as it will need to be manually updated in the future. The year should be dynamically generated or sourced from the event data itself to ensure it stays current.
- `/home/tarcisio/projects/singular/site-frontend/app/pages/agenda-de-eventos/index.vue:68` – The `Carousel` component is imported but never used within the page's template. This unused import should be removed to maintain code cleanliness and avoid confusion.
- `/home/tarcisio/projects/singular/site-frontend/app/pages/agenda-de-eventos/index.vue:74` – The `seo.image` property, used for Open Graph (`og:image`) and Twitter card (`twitter:image`) metadata, is an empty string. This will result in missing preview images when the page is shared on social media, negatively impacting its presentation and click-through rate.
- `/home/tarcisio/projects/singular/site-frontend/app/pages/blog/[slug].vue:153` – The type `any` is used for the `cat` parameter in `fetchedPost.categories?.nodes.map()`. Using `any` undermines the benefits of TypeScript. It is recommended to define a proper type for the category object based on the API's schema to ensure type safety.
- `/home/tarcisio/projects/singular/site-frontend/app/pages/blog/[slug].vue:183` – The meta description is generated in `useHead` by stripping HTML tags from `post.value.description`. This logic is redundant because the `post.description` property in the `post` computed property has already been sanitized in the same way. The code can be simplified by directly using the pre-processed `post.value.description`.
- `/home/tarcisio/projects/singular/site-frontend/app/pages/blog/index.vue:61` – The key `:key="`page-${page}-${index}`" for the pagination list items includes the `index`. While it works here, relying on index in keys is often an anti-pattern. A more stable key could be derived if the `paginationPages` array contained objects with unique properties, which would also make the template logic cleaner.
- `/home/tarcisio/projects/singular/site-frontend/app/pages/blog/index.vue:135` – The `slugify` and `extractDescription` functions are general-purpose utilities defined within the component's setup script. For better code organization and reusability, they should be extracted into a dedicated utility file (e.g., in a `utils` or `composables` directory).
- `/home/tarcisio/projects/singular/site-frontend/app/pages/blog/index.vue:141` – The `extractDescription` function uses a naive regex to strip HTML, which can be unreliable. It also contains a hardcoded fallback string ('Leia o artigo completo...'). This logic should be made more robust (e.g., using a dedicated library for HTML stripping), and presentational text should be moved to the template or a constants/localization file.
- `/home/tarcisio/projects/singular/site-frontend/app/pages/blog/index.vue:146` – The top-level await for `GqlBlog()` lacks explicit error handling. If the promise rejects (e.g., due to a network or GraphQL error), it could crash the component setup. The `GqlBlog` composable should expose an error state that can be checked to render an error message to the user.
- `/home/tarcisio/projects/singular/site-frontend/app/pages/blog/index.vue:163` – Using `new Date().toISOString()` as a fallback for a missing post date is misleading, as it displays the time of rendering instead of indicating that the date is unavailable. It would be better to return `null` and handle the absence of a date in the template.
- `/home/tarcisio/projects/singular/site-frontend/app/pages/blog/index.vue:171` – The `allFoundCategoriesLog` variable is declared but never used. Dead or commented-out code should be removed to improve code clarity.
- `/home/tarcisio/projects/singular/site-frontend/app/pages/blog/index.vue:182` – The `backgroundImage` for a category is sourced from a post within that category. This is non-deterministic, as the image will depend on the order of posts. The category's background image should be a property of the category itself, managed in the CMS.
- `/home/tarcisio/projects/singular/site-frontend/app/pages/contato/orcamento.vue:10` – The `seo.image` property is initialized as an empty string. This will result in empty `og:image` and `twitter:image` meta tags being rendered, which is suboptimal for SEO and social sharing previews. A default fallback image should be provided.
- `/home/tarcisio/projects/singular/site-frontend/app/pages/contato/orcamento.vue:61` – Using `alert()` for user feedback provides a poor and disruptive user experience. It's better to use a more integrated UI element like a toast notification or an inline message within the form to inform the user of the submission status.
- `/home/tarcisio/projects/singular/site-frontend/app/pages/contato/trabalhe-conosco.vue:21` – The 'og:image' and 'twitter:image' meta properties are set to an empty string. This will result in missing images when the page is shared on social media platforms, which can negatively impact engagement. A default fallback image should be provided.
- `/home/tarcisio/projects/singular/site-frontend/app/pages/contato/trabalhe-conosco.vue:67` – The component uses the native browser 'alert()' for user feedback. This is a blocking and stylistically inconsistent UI element that offers a poor user experience. It's recommended to use a dedicated, non-blocking notification component (like a toast or an inline message) for a more modern and user-friendly approach.
- `/home/tarcisio/projects/singular/site-frontend/app/pages/dermatologia/index.vue:30` – Page content is hardcoded directly into the component's script as a large object ('dermatologyData'). This is not scalable or easily maintainable, as content changes require developer intervention and a new deployment. For content-heavy pages, this data should be fetched from a headless CMS or a dedicated API to decouple content from code.
- `/home/tarcisio/projects/singular/site-frontend/app/pages/dermatologia/index.vue:34` – The 'backgroundImage' property is set to a string literal ('banner-dermatologia'), which is likely used as a CSS class name in the child component. This 'magic string' creates an implicit dependency that is not tracked by the build system. A more robust approach would be to import the image asset and pass its URL, making the relationship explicit and preventing breakages if class names or file paths change.
- `/home/tarcisio/projects/singular/site-frontend/app/pages/dermatologia/index.vue:42` – The 'introduction.title' property contains a raw HTML string ('<strong>...</strong>'). This implies the child component ('HealthPage') is using 'v-html' for rendering. While safe here because the content is developer-controlled, it sets a precedent that could lead to Cross-Site Scripting (XSS) vulnerabilities if the data source changes to user- or CMS-generated content in the future. Consider passing structured data and using slots for safer composition.
- `/home/tarcisio/projects/singular/site-frontend/app/pages/formas-farmaceuticas/index.vue:6` – The main content image (`man-holding-a-pill.png`) has an empty `alt` attribute. As this image provides context to the surrounding text, it should have a descriptive alt text to improve accessibility for users relying on screen readers.
- `/home/tarcisio/projects/singular/site-frontend/app/pages/formas-farmaceuticas/index.vue:128` – The `og:image` and `twitter:image` meta properties are initialized with an empty string. This results in a poor visual preview when the page is shared on social media platforms. A relevant image URL should be provided to improve user engagement from social shares.
- `/home/tarcisio/projects/singular/site-frontend/app/pages/formas-farmaceuticas/index.vue:146` – Page content, such as the `carouselItems` array, is hardcoded directly within the Vue component. This practice makes content updates cumbersome, as they require code changes and a new deployment. For better maintainability, this data should be decoupled from the component, for instance by fetching it from a headless CMS or a static JSON file.
- `/home/tarcisio/projects/singular/site-frontend/app/pages/hormonios/index.vue:12` – The `seo.image` property is an empty string. This will result in empty `og:image` and `twitter:image` meta tags, which is suboptimal for social media sharing previews and can be flagged by SEO analysis tools. It's better to provide a default fallback image or conditionally omit the property.
- `/home/tarcisio/projects/singular/site-frontend/app/pages/hormonios/index.vue:15` – The `useHead` composable is used to set the 'robots' meta tag, while `useSeoMeta` is used for all other SEO-related tags. For better code consistency and to centralize SEO management, the robots tag should also be set within the `useSeoMeta` call, e.g., `useSeoMeta({ robots: 'noindex,nofollow', ... })`.
- `/home/tarcisio/projects/singular/site-frontend/app/pages/hormonios/index.vue:44` – The page content is hardcoded as a large static object (`hormonasData`). While this is functional for a static page, it tightly couples content with the component logic. This approach hinders maintainability and scalability, as any content change requires a code modification and redeployment. For larger applications, sourcing this data from a headless CMS or an API is a more idiomatic and flexible approach.
- `/home/tarcisio/projects/singular/site-frontend/app/pages/hormonios/index.vue:50` – The `introduction.title` property contains a raw HTML `<strong>` tag. This implies that the receiving component (`HealthPage`) must use `v-html` to render it. While safe here because the content is static and trusted, it establishes a pattern that can become a security vulnerability (Cross-Site Scripting - XSS) if the data source is ever changed to a user-editable or external source. A safer pattern is to structure the data to represent formatting (e.g., an array of text nodes with a 'bold' flag) and let the component handle the rendering logic.
- `/home/tarcisio/projects/singular/site-frontend/app/pages/index.vue:109` – The `og:image` meta property is set to an empty string on line 109. This will result in poor link previews when sharing the page on social media platforms. A default or specific image URL should be provided to ensure proper social media card rendering.
- `/home/tarcisio/projects/singular/site-frontend/app/pages/index.vue:125` – Large blocks of static content, such as `cards` (line 125), `testimonials` (line 188), and `carouselItems` (line 210), are hardcoded within the component's script. This tightly couples content with presentation, making the component verbose and harder to maintain. This data should be externalized into separate data files or fetched from a headless CMS to improve separation of concerns.
- `/home/tarcisio/projects/singular/site-frontend/app/pages/longevidade/index.vue:35` – All page content is hardcoded within the 'healthspanData' object. While simple, this approach tightly couples content with the component logic, making it difficult for non-developers to manage. For better maintainability and scalability, consider externalizing this content into a separate JSON or TS file, or fetching it from a headless CMS, thus separating content from presentation.
- `/home/tarcisio/projects/singular/site-frontend/app/pages/longevidade/index.vue:44` – The 'introduction.title' property contains a raw HTML string ('<strong>...</strong>'). If the receiving component renders this using 'v-html', it creates a potential Cross-Site Scripting (XSS) vulnerability should the data source ever become dynamic (e.g., from a CMS). The idiomatic and safer approach in Vue is to use component slots for rich text formatting, which separates markup from data.
- `/home/tarcisio/projects/singular/site-frontend/app/pages/martins-brasil/index.vue:3` – The `<img>` tag includes an unnecessary closing tag `</img>`. The `<img>` element is a void element in HTML and should be self-closing (`<img ... />`) or have no closing tag at all. While most browsers will correct this, it is not valid HTML syntax.
- `/home/tarcisio/projects/singular/site-frontend/app/pages/martins-brasil/index.vue:28` – Both `useHead` and `useSeoMeta` are used to set meta tags. The `robots` tag can be set within the `useSeoMeta` composable itself, which would consolidate SEO-related logic into a single function call, making the code cleaner.
- `/home/tarcisio/projects/singular/site-frontend/app/pages/martins-brasil/index.vue:64` – The component uses native `alert()` calls for user feedback. This is a disruptive and poor user experience. It would be better to use a non-blocking, integrated UI component like a toast or an inline message to inform the user of the form submission status.
- `/home/tarcisio/projects/singular/site-frontend/app/pages/martins-brasil/index.vue:69` – The `SectionType` type annotation is used without an import statement. This implies it's a globally defined type. While this may work, it's better practice to explicitly import types to make component dependencies clear and improve code maintainability.
- `/home/tarcisio/projects/singular/site-frontend/app/pages/martins-brasil/index.vue:84` – The `scrollTo` function uses `history.replaceState(null, '', ' ');`, which aggressively modifies the URL by removing the path, query parameters, and hash. This is likely to cause unintended side effects, such as stripping UTM parameters used for analytics or breaking navigation if the user refreshes the page.
- `/home/tarcisio/projects/singular/site-frontend/app/pages/nossa-historia/index.vue:10` – All descriptive text content is hardcoded within the template. This makes content updates require developer intervention and a new deployment. For better maintainability, consider sourcing this content from a headless CMS or a dedicated content file (e.g., JSON).
- `/home/tarcisio/projects/singular/site-frontend/app/pages/nossa-historia/index.vue:48` – The use of `.png` images may not be optimal for performance. Consider compressing these images and using modern, more efficient formats like WebP or AVIF to reduce file size and improve page load speed.
- `/home/tarcisio/projects/singular/site-frontend/app/pages/nossa-historia/index.vue:54` – An `<img>` element on line 54 includes an invalid closing tag (`</img>`). The `<img>` tag is a void element in HTML and should not have a separate closing tag. While browsers may render it correctly, it is syntactically incorrect.
- `/home/tarcisio/projects/singular/site-frontend/app/pages/nossa-historia/index.vue:99` – Idiomatic Issue: Styles are imported from an external, unscoped SCSS file (`import './styles.scss'`). This approach can lead to global style conflicts and makes maintenance harder. The recommended Vue/Nuxt practice is to use `<style scoped>` within the component file to encapsulate styles and prevent them from leaking.
- `/home/tarcisio/projects/singular/site-frontend/app/pages/nossas-unidades/index.vue:12` – The `<img>` tag has an empty `alt` attribute. If this image is not purely decorative and conveys information, it should have descriptive alt text to improve accessibility for users with screen readers.
- `/home/tarcisio/projects/singular/site-frontend/app/pages/nossas-unidades/index.vue:12` – The image `src` uses a relative path ('../../assets/...'). In Nuxt, it is best practice to use the `~/assets` alias to allow the build tool (Vite/Webpack) to process, optimize, and hash the asset for better performance and caching.
- `/home/tarcisio/projects/singular/site-frontend/app/pages/nutricao/index.vue:33` – The entire content for the page is hardcoded in the `nutritionData` object directly within the Vue component. This approach mixes content with presentation logic, making content updates difficult as they require a developer to modify the code and redeploy the application. For better maintainability, this content should be fetched from a headless CMS or a static JSON file loaded via an API route. This also increases the initial JavaScript bundle size for the page.
- `/home/tarcisio/projects/singular/site-frontend/app/pages/politica-de-privacidade/index.vue:29` – The main sections of the document are structured using an `<ol>` element. This is semantically questionable, as an ordered list is typically for a sequence of items, not for sectioning a document. The section numbers are also hardcoded within the `<h2>` tags (e.g., `<h2>1. ...</h2>`), making the `<ol>` element's numbering capability redundant. A more semantic approach would be to use `<section>` elements for each major topic, which improves the document outline and accessibility.
- `/home/tarcisio/projects/singular/site-frontend/app/pages/politica-de-privacidade/index.vue:122` – The `seo.image` variable is initialized as an empty string. This will generate meta tags like `<meta property="og:image" content="">`, which is ineffective for social media crawlers and results in a poor appearance when the page is shared. It's recommended to provide a default fallback image for the site or conditionally omit the `ogImage` and `twitterImage` properties if no specific image is available for this page.
- `/home/tarcisio/projects/singular/site-frontend/app/pages/saude-da-mulher/index.vue:34` – The page content is hardcoded directly within the component's script. For a scalable and maintainable application, this data should be fetched from a headless CMS or an API. This approach makes content updates difficult, requiring code changes and a new deployment.
- `/home/tarcisio/projects/singular/site-frontend/app/pages/saude-da-mulher/index.vue:47` – The 'introduction.title' data property contains a raw HTML '<strong>' tag. This practice couples the data to a specific HTML structure and forces the consuming component to use 'v-html', which can be a security risk (XSS) if the content were dynamic. A better approach is to pass structured data and let the component handle its own markup for styling.
- `/home/tarcisio/projects/singular/site-frontend/app/pages/saude-do-homem/index.vue:8` – The 'robots' meta tag is hardcoded to 'noindex,nofollow'. While this may be intentional for staging environments, it will prevent this page from being indexed by search engines in production, which is likely not the desired behavior for a public-facing page.
- `/home/tarcisio/projects/singular/site-frontend/app/pages/saude-do-homem/index.vue:33` – All page content is hardcoded as a large static object (`menHealthData`) within the component script. This is an idiomatic issue regarding maintainability. It tightly couples content with presentation logic, requiring developer intervention and a new deployment for any text change. A more scalable approach would be to fetch this content from a headless CMS or a separate content file (e.g., JSON).
- `/home/tarcisio/projects/singular/site-frontend/app/pages/saude-do-homem/index.vue:43` – The `introduction.title` string contains a raw HTML `<strong>` tag. Rendering this in the child component likely requires `v-html`, which can introduce a Cross-Site Scripting (XSS) vulnerability if the data source becomes dynamic or less trusted in the future. A safer pattern is to structure the data to separate content from presentation, avoiding HTML in strings.
- `/home/tarcisio/projects/singular/site-frontend/app/pages/saude-intestinal/index.vue:5` – The page imports an image asset directly. While functional, this misses the opportunity to use Nuxt's image optimization features (like '@nuxt/image') which can provide automatic resizing, format conversion (e.g., to WebP), and lazy loading, leading to better performance and lower bandwidth usage.
- `/home/tarcisio/projects/singular/site-frontend/app/pages/saude-intestinal/index.vue:48` – The 'introduction.title' contains a raw HTML '<strong>' tag. This forces the child component ('HealthPage') to use 'v-html' to render it, which can be a security risk (XSS) if the content source is ever changed to user input or a less trusted API. It is better to handle rich text using component slots or a structured format that doesn't rely on 'v-html'.
- `/home/tarcisio/projects/singular/site-frontend/app/pages/seja-um-franqueado/index.vue:11` – The `<img>` tag has an empty `alt` attribute. If the image is not purely decorative and conveys meaningful content, it must have descriptive alt text for accessibility compliance (WCAG).
- `/home/tarcisio/projects/singular/site-frontend/app/pages/seja-um-franqueado/index.vue:35` – The `<img>` tag has an empty `alt` attribute. If the image is not purely decorative and conveys meaningful content, it must have descriptive alt text for accessibility compliance (WCAG).
- `/home/tarcisio/projects/singular/site-frontend/app/pages/seja-um-franqueado/index.vue:47` – Using the loop `index` as the `:key` for a `v-for` directive is an anti-pattern. If the `advantages` array were to be non-static (e.g., reordered, filtered), it could lead to unpredictable UI behavior and inefficient DOM updates. A unique identifier from each `advantage` item should be used instead.
- `/home/tarcisio/projects/singular/site-frontend/app/pages/seja-um-franqueado/index.vue:112` – The `seo.image` constant used for `og:image` and `twitter:image` meta tags is an empty string. This will result in missing or poorly rendered previews when the page is shared on social media platforms.
- `/home/tarcisio/projects/singular/site-frontend/app/pages/seja-um-franqueado/index.vue:148` – The native `alert()` function is used for user feedback upon form submission. This is a blocking operation and provides a poor, stylistically inconsistent user experience. A non-blocking notification component (e.g., a toast or an inline message) is recommended.
- `/home/tarcisio/projects/singular/site-frontend/app/utils/formatDate.ts:8` – The guard clause `if (!dateInput)` treats the number `0` as a falsy value and returns an empty string. However, `0` is a valid timestamp representing the Unix epoch and should be formatted correctly. The check should be more specific, like `if (dateInput == null)`.
- `/home/tarcisio/projects/singular/site-frontend/app/utils/formatDate.ts:18` – A new `Intl.DateTimeFormat` instance is created on every function call. In performance-critical scenarios where this function is called frequently (e.g., rendering a large list), this is inefficient. The formatter instance should be cached at the module level for reuse.
- `/home/tarcisio/projects/singular/site-frontend/app/utils/formatDate.ts:27` – The guard clause `if (!dateInput)` treats the number `0` as a falsy value and returns an empty string. However, `0` is a valid timestamp representing the Unix epoch and should be formatted correctly. The check should be more specific, like `if (dateInput == null)`.
- `/home/tarcisio/projects/singular/site-frontend/app/utils/getImgUrl.ts:5` – The function uses a boolean flag `icon` to switch between two different behaviors (fetching icons vs. images). This is a 'boolean trap' code smell which can make the code harder to understand and maintain. Consider creating two separate, more specific functions, such as `getIconUrl(name)` and `getImageUrl(name)`.
- `/home/tarcisio/projects/singular/site-frontend/app/utils/getImgUrl.ts:11` – The function repeatedly uses the type assertion `as { default: string }`. This suggests that the types from `import.meta.glob` are not strongly typed. It would be better to explicitly type the imported modules once, for example: `const icons: Record<string, { default: string }> = import.meta.glob(...)`. This improves type safety and code readability.
- `/home/tarcisio/projects/singular/site-frontend/app/utils/slugify.ts:14` – The `.toString()` method is called on the `text` parameter, which is already typed as a `string`. This call is redundant and has no effect on the outcome. It can be safely removed for cleaner code.
- `/home/tarcisio/projects/singular/site-frontend/app/utils/slugify.ts:18` – The regular expression `/[^\w\s-]/g` does not account for underscores (`_`), as `\w` includes them. This results in slugs that may contain a mix of hyphens and underscores (e.g., 'hello_world' remains unchanged), which is often undesirable for URL consistency. Consider adding a `.replace(/_/g, '-')` to standardize all word separators to hyphens.
- `/home/tarcisio/projects/singular/site-frontend/app/utils/types.ts:1` – The file inconsistently uses both `type` aliases and `interface` declarations for defining object shapes. While functionally similar for this use case, standardizing on one (typically `interface` for objects, as it can be extended) improves code consistency and maintainability.
- `/home/tarcisio/projects/singular/site-frontend/app/utils/types.ts:9` – Property names for image sources are inconsistent across types (e.g., `backgroundImageUrl`, `productImage`, `imgUrl`, `backgroundImage`). Adopting a consistent naming convention, such as always using a suffix like `Url` or `Src` (e.g., `productImageUrl`, `imageUrl`), would improve code clarity and predictability.
- `/home/tarcisio/projects/singular/site-frontend/app/utils/types.ts:26` – The type `SectionType` uses a redundant `Type` suffix. In a TypeScript file, it is already clear that this is a type definition. A more idiomatic and concise name would be `Section`.

