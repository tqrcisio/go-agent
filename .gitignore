# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib
.env
# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with the
# -o option to specify a file path
*.out

# Dependency directories (remove the comment below to include it)
# vendor/

# Go workspace file
go.work
go.work.sum
/go-agent

# go-semantic-release leaves these in the working tree; goreleaser then
# refuses to run on a dirty checkout.
.semrel/
.generated-go-semantic-release-changelog.md
